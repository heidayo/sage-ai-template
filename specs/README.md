# Specs

SPEC-XXXX 形式の仕様書を格納するディレクトリ。

## 使い方

1. `_template.md` をコピーして新しいSPECを作成
2. `make id-gen TYPE=spec` で次のIDを取得
3. すべてのフィールドを埋める（スコープ外・異常系・受け入れ条件は必須）
4. ステータスを `Draft` → `Review` → `Approved` と進める

## ルール

- 仕様なしの実装は禁止
- 受け入れ条件は最低3件、コマンドで検証可能な形式
- スコープ外は「なし」不可（意識的に除外範囲を記述）
