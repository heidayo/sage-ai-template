#!/usr/bin/env bash
# TASK-0207: mock tsc fixture — 0 errors (clean project, boundary case 1).
# Prints no `error TS` lines and exits 0, like tsc on a clean project.
printf 'Files:              12\n'
exit 0
